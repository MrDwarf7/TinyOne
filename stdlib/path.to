# tinyone-stdlib/path.to
# Linux-first path manipulation. Pure string ops, no FS access.

export fn join(left, right) {
  return path_join(left, right)
}

export fn basename(path) {
  return path_basename(path)
}

export fn dirname(path) {
  return path_dirname(path)
}
